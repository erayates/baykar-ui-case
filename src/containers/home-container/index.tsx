import Button from "@/components/ui/button";

const HomeContainer: React.FC = () => {
  return <Button variant="outlined" color="secondary" size="small">Sign up now</Button>;
};

export default HomeContainer;
